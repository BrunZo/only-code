[build-system]
requires = ["scikit-build-core", "nanobind"]
build-backend = "scikit_build_core.build"

[project]
name = "data_structures"
version = "0.1.0"

[tool.scikit-build]
wheel.packages = ["data_structures"]
